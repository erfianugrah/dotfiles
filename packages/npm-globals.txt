# npm global packages (cross-platform)
# Generated by: save_packages (2026-07-15)
# Update with: save_packages
@anthropic-ai/claude-code
# Pinned: 2026.7.0's WASM core crashes `bw serve` on some vault items
# (invalid type: Object, expected a string) and unlock fails intermittently.
# 2026.6.0 is the last good release. Managed via npm (not brew) so the pin holds.
@bitwarden/cli@2026.6.0
@google/gemini-cli
@mermaid-js/mermaid-cli
@openai/codex
acorn
eslint
gtop
prettier
typescript
vercel
wrangler
